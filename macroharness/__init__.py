"""macro-harness: micro-harness plus the layers it deliberately left out.

micro-harness is one loop, one file, three tools. macro-harness keeps that loop
and gives each missing layer its own module, small enough to read in a sitting.
"""

__version__ = "0.1.0"
